
  <div class="border mx-auto">
    <div class=" bg-slate-500/75 flex flex-row justify-between align-text-bottom">
      <span class="flex flex-row pt-1 pl-2 cursor-pointer select-none hover:underline hover:text-white text-sm" on:click={btnRefresh}>
        Refresh
      </span>
      <span class="flex flex-row pt-1 pr-2 select-none hover:underline hover:text-white cursor-pointer" on:click={btnCreate}>
        <PencilSquare class="w-4 h-4" />
      </span>
    </div>
    <div class="overflow-clip bg-white">
      {#await data}
        <div class="cursor-default select-none">
          <div class="text-center animate-ping">loading...</div>
        </div>
      {:then data}
        {#each data.data as i (i.hash)}
          <div
            class="cursor-default select-none border bg-white hover:bg-stone-300/50 transition duration-500 ease-in hover:ease-out hover:duration-100 hover:scale-105"
            on:mousedown={(i) => {
              selected = i.target.parentElement.id;
            }}
            id={i.hash}
          >
            <span class="py-1 px-4">
              {i.subject}
            </span>
            <span class="py-1 px-4">
              {i.displayname}
            </span>
          </div>
        {/each}
      {/await}
    </div>
  </div>
